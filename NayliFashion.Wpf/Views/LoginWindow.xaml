<Window x:Class="NayliFashion.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:NayliFashion.Wpf.Converters"
        mc:Ignorable="d"
        Title="Nayli Fashion - تسجيل الدخول للنظام"
        Height="500" Width="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        Background="{StaticResource BrushBackground}">

    <Window.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <!-- شعار البرنامج -->
            <Border Width="70" Height="70" Background="{StaticResource BrushCard}" CornerRadius="35" HorizontalAlignment="Center" Margin="0,0,0,16" BorderBrush="{StaticResource BrushAccentGold}" BorderThickness="2">
                <TextBlock Text="NF" Foreground="{StaticResource BrushAccentGold}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>

            <TextBlock Text="Nayli Fashion - نايل فاشن"
                       Foreground="{StaticResource BrushAccentGold}"
                       FontSize="22"
                       FontWeight="Bold"
                       HorizontalAlignment="Center" />

            <TextBlock Text="نظام إدارة المبيعات والكراء والمخزون الشامل"
                       Foreground="{StaticResource BrushTextMuted}"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,24" />

            <!-- رسالة الخطأ إن وجدت -->
            <Border Background="{StaticResource BrushDanger}"
                    CornerRadius="8"
                    Padding="10"
                    Margin="0,0,0,14"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#FFFFFF" FontWeight="Bold" FontSize="12" TextAlignment="Center" />
            </Border>

            <!-- حقل اسم المستخدم -->
            <TextBlock Text="اسم المستخدم:" Foreground="{StaticResource BrushTextSecondary}" FontSize="12" Margin="0,0,0,6" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="40" FontSize="14" Margin="0,0,0,14" />

            <!-- حقل كلمة المرور -->
            <TextBlock Text="كلمة المرور:" Foreground="{StaticResource BrushTextSecondary}" FontSize="12" Margin="0,0,0,6" />
            <PasswordBox x:Name="TxtPassword"
                         PasswordChanged="TxtPassword_PasswordChanged"
                         Background="{StaticResource BrushSurface}"
                         Foreground="{StaticResource BrushTextPrimary}"
                         CaretBrush="{StaticResource BrushAccentGold}"
                         BorderBrush="{StaticResource BrushBorder}"
                         BorderThickness="1"
                         Padding="12,10"
                         FontSize="14"
                         Height="40"
                         Margin="0,0,0,24">
                <PasswordBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8" />
                    </Style>
                </PasswordBox.Resources>
            </PasswordBox>

            <!-- زر تسجيل الدخول -->
            <Button Content="تسجيل الدخول إلى النظام"
                    Command="{Binding LoginCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Height="46"
                    FontSize="15"
                    FontWeight="Bold" />

            <TextBlock Text="بيانات الدخول الافتراضية: admin / admin123"
                       Foreground="{StaticResource BrushTextMuted}"
                       FontSize="11"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,0" />
        </StackPanel>
    </Grid>
</Window>
